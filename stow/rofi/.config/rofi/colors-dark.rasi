* {
    bg: #222226;
    fg: #f1f1f1;
    accent: #bda7f0;
    muted: #6a6a6a;
    selection: #4d3e66;
}

/* highlight can't take a variable, so the accent is repeated here. */
element-text {
    highlight: bold #bda7f0;
}
